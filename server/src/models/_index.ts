async function createTables(): Promise<void> {
	// const conn = await createConnection();
	// const usersSchema = fs.readFileSync("users.sql", "utf8");
	// const postsSchema = fs.readFileSync("posts.sql", "utf8");
	// await conn.execute(usersSchema);
	// await conn.execute(postsSchema);
	// conn.release();
}
